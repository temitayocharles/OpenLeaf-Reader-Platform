name: CD Release

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Target environment overlay for Helm render validation"
        required: true
        default: "staging"
        type: choice
        options:
          - dev
          - staging
          - prod
      image_tag:
        description: "Optional image tag override (defaults to git tag or short SHA)"
        required: false
        type: string
      promote_gitops:
        description: "Open a GitOps PR with updated OpenLeaf image tags"
        required: false
        default: false
        type: boolean
      gitops_repo:
        description: "GitOps repo in owner/name format"
        required: false
        default: "temitayocharles/platform-gitops"
        type: string
      gitops_base_branch:
        description: "Target base branch for GitOps PR"
        required: false
        default: "main"
        type: string
  push:
    tags:
      - "staging-*"
      - "prod-*"
      - "release-*"

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ghcr.io/${{ github.repository_owner }}

jobs:
  helm-validate:
    runs-on: ubuntu-latest
    outputs:
      deploy_env: ${{ steps.vars.outputs.deploy_env }}
      release_tag: ${{ steps.vars.outputs.release_tag }}
      promote_gitops: ${{ steps.vars.outputs.promote_gitops }}
      gitops_repo: ${{ steps.vars.outputs.gitops_repo }}
      gitops_base_branch: ${{ steps.vars.outputs.gitops_base_branch }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Resolve release variables
        id: vars
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            DEPLOY_ENV="${{ github.event.inputs.environment }}"
            PROMOTE_GITOPS="${{ github.event.inputs.promote_gitops }}"
            GITOPS_REPO="${{ github.event.inputs.gitops_repo }}"
            GITOPS_BASE_BRANCH="${{ github.event.inputs.gitops_base_branch }}"
            if [ -n "${{ github.event.inputs.image_tag }}" ]; then
              RELEASE_TAG="${{ github.event.inputs.image_tag }}"
            else
              RELEASE_TAG="${GITHUB_SHA::7}"
            fi
          else
            REF_TAG="${GITHUB_REF_NAME}"
            RELEASE_TAG="${REF_TAG}"
            PROMOTE_GITOPS="false"
            GITOPS_REPO="temitayocharles/platform-gitops"
            GITOPS_BASE_BRANCH="main"
            case "${REF_TAG}" in
              prod-*) DEPLOY_ENV="prod" ;;
              release-*) DEPLOY_ENV="prod" ;;
              staging-*) DEPLOY_ENV="staging" ;;
              *) DEPLOY_ENV="staging" ;;
            esac
          fi
          echo "deploy_env=${DEPLOY_ENV}" >> "${GITHUB_OUTPUT}"
          echo "release_tag=${RELEASE_TAG}" >> "${GITHUB_OUTPUT}"
          echo "promote_gitops=${PROMOTE_GITOPS}" >> "${GITHUB_OUTPUT}"
          echo "gitops_repo=${GITOPS_REPO}" >> "${GITHUB_OUTPUT}"
          echo "gitops_base_branch=${GITOPS_BASE_BRANCH}" >> "${GITHUB_OUTPUT}"

      - name: Setup Helm
        uses: azure/setup-helm@v4

      - name: Lint Helm charts
        run: |
          helm lint helm-charts/charts/openleaf-config
          helm lint helm-charts/charts/openleaf-user-service
          helm lint helm-charts/charts/openleaf-services-bundle
          helm lint helm-charts/charts/openleaf-gateway

      - name: Render Helm charts
        shell: bash
        run: |
          ENV_FILE="values-${{ steps.vars.outputs.deploy_env }}.yaml"
          helm template openleaf-config helm-charts/charts/openleaf-config -f "helm-charts/charts/openleaf-config/${ENV_FILE}" > /tmp/openleaf-config.render.yaml
          helm template openleaf-user helm-charts/charts/openleaf-user-service -f "helm-charts/charts/openleaf-user-service/${ENV_FILE}" > /tmp/openleaf-user.render.yaml
          helm template openleaf-services helm-charts/charts/openleaf-services-bundle -f "helm-charts/charts/openleaf-services-bundle/${ENV_FILE}" > /tmp/openleaf-services.render.yaml
          helm template openleaf-gateway helm-charts/charts/openleaf-gateway -f "helm-charts/charts/openleaf-gateway/${ENV_FILE}" > /tmp/openleaf-gateway.render.yaml

      - name: Upload Helm render artifacts
        uses: actions/upload-artifact@v4
        with:
          name: helm-render-${{ steps.vars.outputs.deploy_env }}-${{ steps.vars.outputs.release_tag }}
          path: |
            /tmp/openleaf-config.render.yaml
            /tmp/openleaf-user.render.yaml
            /tmp/openleaf-services.render.yaml
            /tmp/openleaf-gateway.render.yaml

  build-and-push:
    needs: helm-validate
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: user-service
            context: services/user-service
            image: openleaf-user-service
          - name: book-service
            context: services/book-service
            image: openleaf-book-service
          - name: publishing-service
            context: services/publishing-service
            image: openleaf-publishing-service
          - name: subscription-service
            context: services/subscription-service
            image: openleaf-subscription-service
          - name: payment-service
            context: services/payment-service
            image: openleaf-payment-service
          - name: analytics-service
            context: services/analytics-service
            image: openleaf-analytics-service
          - name: frontend
            context: frontend
            image: openleaf-frontend
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup QEMU
        uses: docker/setup-qemu-action@v3

      - name: Setup Buildx
        uses: docker/setup-buildx-action@v3

      - name: Resolve GHCR credentials
        id: ghcr-creds
        shell: bash
        env:
          GHCR_TOKEN: ${{ secrets.GHCR_TOKEN }}
          GHCR_USERNAME: ${{ secrets.GHCR_USERNAME }}
        run: |
          if [ -n "${GHCR_TOKEN}" ]; then
            if [ -n "${GHCR_USERNAME}" ]; then
              USERNAME="${GHCR_USERNAME}"
            else
              USERNAME="${GITHUB_ACTOR}"
            fi
            PASSWORD="${GHCR_TOKEN}"
          else
            USERNAME="${GITHUB_ACTOR}"
            PASSWORD="${{ secrets.GITHUB_TOKEN }}"
          fi
          echo "username=${USERNAME}" >> "${GITHUB_OUTPUT}"
          echo "password=${PASSWORD}" >> "${GITHUB_OUTPUT}"

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ steps.ghcr-creds.outputs.username }}
          password: ${{ steps.ghcr-creds.outputs.password }}

      - name: Build and push multi-arch image
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.context }}
          push: true
          platforms: linux/amd64,linux/arm64
          tags: |
            ${{ env.IMAGE_PREFIX }}/${{ matrix.image }}:${{ needs.helm-validate.outputs.release_tag }}
            ${{ env.IMAGE_PREFIX }}/${{ matrix.image }}:${{ needs.helm-validate.outputs.deploy_env }}-latest

  release-evidence:
    needs:
      - helm-validate
      - build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download Helm renders
        uses: actions/download-artifact@v4
        with:
          name: helm-render-${{ needs.helm-validate.outputs.deploy_env }}-${{ needs.helm-validate.outputs.release_tag }}
          path: /tmp/helm-render

      - name: Build release evidence
        shell: bash
        run: |
          sha256sum /tmp/helm-render/*.yaml > /tmp/render-checksums.txt
          cat > /tmp/release-evidence.md <<EOF
          # OpenLeaf CD Evidence

          - commit: ${GITHUB_SHA}
          - deploy_env: ${{ needs.helm-validate.outputs.deploy_env }}
          - image_tag: ${{ needs.helm-validate.outputs.release_tag }}
          - image_prefix: ${{ env.IMAGE_PREFIX }}

          ## Published Images
          - ${{ env.IMAGE_PREFIX }}/openleaf-user-service:${{ needs.helm-validate.outputs.release_tag }}
          - ${{ env.IMAGE_PREFIX }}/openleaf-book-service:${{ needs.helm-validate.outputs.release_tag }}
          - ${{ env.IMAGE_PREFIX }}/openleaf-publishing-service:${{ needs.helm-validate.outputs.release_tag }}
          - ${{ env.IMAGE_PREFIX }}/openleaf-subscription-service:${{ needs.helm-validate.outputs.release_tag }}
          - ${{ env.IMAGE_PREFIX }}/openleaf-payment-service:${{ needs.helm-validate.outputs.release_tag }}
          - ${{ env.IMAGE_PREFIX }}/openleaf-analytics-service:${{ needs.helm-validate.outputs.release_tag }}
          - ${{ env.IMAGE_PREFIX }}/openleaf-frontend:${{ needs.helm-validate.outputs.release_tag }}

          ## Helm Render Checksums
          \`\`\`
          $(cat /tmp/render-checksums.txt)
          \`\`\`
          EOF

      - name: Upload release evidence
        uses: actions/upload-artifact@v4
        with:
          name: release-evidence-${{ needs.helm-validate.outputs.deploy_env }}-${{ needs.helm-validate.outputs.release_tag }}
          path: |
            /tmp/release-evidence.md
            /tmp/render-checksums.txt

  promote-gitops:
    needs:
      - helm-validate
      - build-and-push
      - release-evidence
    if: needs.helm-validate.outputs.promote_gitops == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Checkout GitOps repo
        uses: actions/checkout@v4
        with:
          repository: ${{ needs.helm-validate.outputs.gitops_repo }}
          ref: ${{ needs.helm-validate.outputs.gitops_base_branch }}
          token: ${{ secrets.GITOPS_REPO_TOKEN }}
          path: gitops

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install update dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyyaml

      - name: Update OpenLeaf image tags in values files
        shell: bash
        run: |
          python3 - <<'PY'
          import os
          from pathlib import Path
          import yaml

          root = Path("gitops")
          env_name = "${{ needs.helm-validate.outputs.deploy_env }}"
          tag = "${{ needs.helm-validate.outputs.release_tag }}"

          target_name = f"values-{env_name}.yaml"
          targets = list(root.rglob(target_name))
          changed = []

          def update_image_block(block):
            if not isinstance(block, dict):
              return 0
            repo = block.get("repository")
            if isinstance(repo, str) and "/openleaf-" in repo:
              old = block.get("tag")
              if old != tag:
                block["tag"] = tag
                return 1
            return 0

          for path in targets:
            try:
              data = yaml.safe_load(path.read_text()) or {}
            except Exception:
              continue

            updates = 0
            if isinstance(data, dict):
              updates += update_image_block(data.get("image"))
              services = data.get("services")
              if isinstance(services, dict):
                for svc in services.values():
                  if isinstance(svc, dict):
                    updates += update_image_block(svc.get("image"))

            if updates > 0:
              path.write_text(yaml.safe_dump(data, sort_keys=False))
              changed.append((str(path), updates))

          if not changed:
            print("No OpenLeaf image tags were updated in GitOps values files.")
          else:
            print("Updated files:")
            for path, updates in changed:
              print(f"- {path} ({updates} image blocks)")
          PY

      - name: Create pull request in GitOps repo
        uses: peter-evans/create-pull-request@v7
        with:
          token: ${{ secrets.GITOPS_REPO_TOKEN }}
          path: gitops
          branch: openleaf/cd-${{ needs.helm-validate.outputs.deploy_env }}-${{ needs.helm-validate.outputs.release_tag }}
          base: ${{ needs.helm-validate.outputs.gitops_base_branch }}
          commit-message: "chore(gitops): promote OpenLeaf images to ${{ needs.helm-validate.outputs.release_tag }}"
          title: "chore(gitops): promote OpenLeaf images to ${{ needs.helm-validate.outputs.release_tag }} (${{ needs.helm-validate.outputs.deploy_env }})"
          body: |
            Automated promotion from OpenLeaf CD pipeline.

            - Environment: `${{ needs.helm-validate.outputs.deploy_env }}`
            - Image tag: `${{ needs.helm-validate.outputs.release_tag }}`
            - Source commit: `${{ github.sha }}`

            This PR updates `values-${{ needs.helm-validate.outputs.deploy_env }}.yaml` files that contain OpenLeaf image repositories.
